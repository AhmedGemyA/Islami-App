import 'package:flutter/material.dart';

class Qurandetalesview extends StatelessWidget {
  const Qurandetalesview({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Color(0xFF202020),
      appBar: AppBar(title: Text("الفاتحه")),
    );
  }
}
